package com.adminsys.code.字符串解码;

class Solution {
    public String decodeString(String s) {
        for (int i = 0; i < s.length(); i++) {
            char ch = s.charAt(i);
        }
        return "";
    }
}